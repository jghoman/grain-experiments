let doMath = (fn, x, y) => fn(x, y)

let multiply = (x, y) => x * y
let subtract = (x, y) => x - y

print(doMath(multiply, 4, 6))

let rec fibonacci = (n) => {
    if (n == 0 || n == 1) {
        n
    } else {
        fibonacci(n - 1) + fibonacci(n - 2)
    }
}

print(fibonacci(50))
